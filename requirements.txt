# NanoBot Voice Control - Python Requirements
# Install with: pip install -r requirements.txt

# Speech recognition (offline Whisper model)
openai-whisper>=20231117

# HTTP requests to ESP32
requests>=2.28.0

# Audio recording from microphone
pyaudio>=0.2.13

# Numerical processing for audio data
numpy>=1.24.0

# Standard library modules (no install needed):
# - os, time, threading, queue